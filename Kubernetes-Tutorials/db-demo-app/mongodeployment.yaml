apiVersion: apps/v1
kind: Deployment
metadata:
  name: mongo
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mongo
  template:
    metadata:
      labels:
        app: mongo
    spec:
      containers:
      - name: mongo
        image: mongo:latest
        ports:
        - containerPort: 27017
<<<<<<< HEAD
        volumeMounts:
          - mountPath: /data/db
            name: mongo-vol
      volumes:
        - name: mongo-vol
          persistentVolumeClaim:
            claimName: nodeapp-mongo-pvc

=======
        env: 
          - name: MONGO_INITDB_ROOT_USERNAME
            valueFrom:
              secretKeyRef:
                name: mongo-sec
                key: username
          - name: MONGO_INITDB_ROOT_PASSWORD
            valueFrom:
              secretKeyRef:
                name: mongo-sec
                key: password
>>>>>>> da8b220843663daf099cdae281f7df01fe53e203

---

apiVersion: v1
kind: Service
metadata:
  name: mongo-nodeapp-service
spec: 
  ports:
    - name: tcp
      port: 27017
      targetPort: 27017
  selector:
    app: mongo
